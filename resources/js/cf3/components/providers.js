define(['react', 'components/page_header'], function(React, PageHeader) {
    return React.createClass({
        render: function() {
            return React.DOM.div({}, "Coming soon");
        }
    });
});
